{
  "name": "next-xz",
  "version": "0.1.0-draft",
  "private": true,
  "description": "Next.js + Xz orchestration toolkit: AI-written, human-reviewed backend logic.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check:json": "xz check-json",
    "build:shared": "xz build --shared",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  },
  "keywords": [
    "nextjs",
    "xz",
    "ffi",
    "agent",
    "audit",
    "ai-written-human-reviewed"
  ]
}
